{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sass:build": "cross-env sass --source-map --update --style=compressed .",
    "sass:watch": "cross-env sass --source-map-urls absolute --update --style=compressed --watch ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.7",
    "sass": "^1.32.11",
    "scss": "^0.2.4"
  }
}
